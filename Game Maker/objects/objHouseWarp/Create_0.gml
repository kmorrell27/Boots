event_inherited(); //Call the parent's event.
y -= 3; //Shift this object up 3 pixels.
transition = Transition.RECTANGLE; //Use the Rectangle transition.
transitionframes = global.onesecond * 2; //More transition frames.
entersnd = -1; //Don't play a sound effect when touched.
